<h1>List of users</h1>
<table border="1">
<tr>

<th>Id</th>
<th>Username</th>
<th></th>
</tr>

<% @list_of_users.each do |a_user| %>
<tr>

<td><%= a_user.id %></td>
<td><%= a_user.username %></td>
<td><a href="/users/<%= a_user.username %>"> Show details </a>
</td>

</tr>
<% end %>
</table>

<h1>List of users</h1>
<table border="1">
  <tr>
    <th>Id</th>
    <th>Username</th>
    <th></th>
  </tr>

  <% @list_of_users.each do |a_user| %>
  <tr>
    <td><%= a_user.id %></td>
    <td><%= a_user.username %></td>
    <td><a href="/users/<%= a_user.username %>">Show details</a></td>
  </tr>
  <% end %>
</table>

<h2>Add a new user</h2>
<form action="/insert_user" method="post">
  <label for="username_box">Username</label>
  <input id="username_box" name="query_username" type="text">

  <button type="submit">Add user</button>
</form>

